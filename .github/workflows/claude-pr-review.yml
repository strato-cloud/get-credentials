name: Claude PR Review
on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

jobs:
  call:
    uses: strato-cloud/automation/.github/workflows/claude-pr-review.yml@v1
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    secrets: inherit