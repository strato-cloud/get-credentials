name: Claude Work Issue
on:
  issues:
    types: [opened]

jobs:
  call:
    uses: strato-cloud/automation/.github/workflows/claude-work-issue.yml@v1
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write
    secrets: inherit