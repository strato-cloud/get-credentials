name: Claude Address Review
on:
  pull_request_review:
    types: [submitted]

jobs:
  call:
    uses: strato-cloud/automation/.github/workflows/claude-address-review.yml@v1
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write
    secrets: inherit